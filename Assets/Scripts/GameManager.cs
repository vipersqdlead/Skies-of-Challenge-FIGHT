using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{

    public int Score = 0;

    public List<GameObject> Enemies = new List<GameObject>(); // List of enemies present on the battle.

    public GameObject He280Prefab;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
